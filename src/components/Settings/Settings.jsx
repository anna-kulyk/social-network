import React from 'react';
import './Settings.scss';

const Settings = () => {
    return (
        <div className='settings _section _page-section'>
            Settings
        </div>
    );
};

export default Settings;